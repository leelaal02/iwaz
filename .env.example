# 이 파일을 복사해 `.env`로 만들고 실제 키를 채우세요:  cp .env.example .env
# `.env`는 .gitignore에 있어 커밋되지 않습니다. 키를 여기(.example)에 넣지 마세요.

# 클라우드 STT(Groq) 사용 시 필요. https://console.groq.com/keys 에서 발급.
GROQ_API_KEY=your-groq-api-key-here
# 여기에 키 넣으면 안됨, .env 파일에 넣기